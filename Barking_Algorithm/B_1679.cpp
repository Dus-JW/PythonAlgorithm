#include <bits/stdc++.h>
using namespace std;

int n,m;
int	count;

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> n >> m;
	while (n != m)
	{
		if (m / 2 > n)
			n *= 2;
		else if (n > m)
	}
	return (0);
}
